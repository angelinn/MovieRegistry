<h2> Results: </h2>

<% movies.each do |movie| %>
  <a href="/movie?id=<%= movie.id %>"><%= movie.title %> </a><br />
  <!-- <a href="/add?id=<%= movie.id %>&series=<%= movie.title.include?('Series') %>"><%= movie.title %> </a><br /> -->
<% end %>
